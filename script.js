const bigFiveButton = document.querySelectorAll(".big-five-button")

// bigFiveButton.forEach( () => {
//     button.addEventListener("click", bigFiveSpotted)
// })


bigFiveButton.addEventListener("click",bigFiveSpotted)

function bigFiveSpotted(animal) {
    console.log("works")
}

